<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="joslabs.companyx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Sales person"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:label="Sales"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <activity
            android:name=".pending.PendingActivity"
            android:label="@string/title_activity_pending"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".productdistribution.Productdistribution"
            android:label="@string/title_activity_productdistribution"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".chats.ChatsActivity"
            android:label="@string/title_activity_chats"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".analysis.AnalysisActivity"
            android:label="@string/title_activity_analysis"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".todaycash.todaypayments"
            android:label="@string/title_activity_todaypayments"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".viewsperson.Viewsperson"
            android:label="@string/title_activity_viewsperson"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".clientlist.ClientlistActivity"
            android:label="@string/title_activity_clientlist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".salesrecords.SalesrecordsActivity"
            android:label="@string/title_activity_salesrecords"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AppManager"
            android:label="System App Manager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".chatsreal.ChatsrealActivity"
            android:label="chats"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewSalesKey"
            android:label="@string/title_activity_new_sales_key"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".viewsperson.Viewsalesmap"
            android:label="@string/title_activity_viewsalesmap"></activity>
    </application>

</manifest>